#include <stdio.h>
#include <stdlib.h>

int main (){
    float area,lado;
        printf("Informe o valor do lado: ");
        scanf("%f",&lado);
           area=lado*lado;
        printf("Valor da area e: %.2f \n",area);


system ("pause");
return 0;
}
